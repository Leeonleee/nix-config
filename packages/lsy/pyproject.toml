[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lsy"
version = "0.1.0"
description = "Personal lsylabs utilities"
requires-python = ">=3.11"

[project.scripts]
lsy = "lsy.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
